#ifndef DEF_SegmentTracker
#define DEF_SegmentTracker


using namespace cv;

// code pas configuré et pas très utile de base
// mais à garder :D


        // cvtColor(rotated, gray, CV_BGR2GRAY);
        // Mat matlines = gray.clone();
        // Canny( matlines, matlines, 50, 200, 3 );
        // GaussianBlur( matlines, matlines, Size(9, 9), 2, 2 );
        // imshow("canny", matlines);
        // vector<Vec4i> lines;
        // HoughLinesP(matlines, lines, 1, CV_PI/180, 50, 50, 10 );
        // for( size_t i = 0; i < lines.size(); i++ )
        // {
        //     Vec4i l = lines[i];
        //     line( rotated, Point(l[0], l[1]), Point(l[2], l[3]), Scalar(0,0,255), 3, CV_AA);
        // }


#endif
